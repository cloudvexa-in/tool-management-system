# Local Postgres, matches docker-compose.yml (docker compose up -d)
DATABASE_URL="postgresql://onescreen:onescreen@localhost:5434/onescreen?schema=public"

# next-auth
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
